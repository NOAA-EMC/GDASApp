covariance model: SABER
saber central block:
  saber block name: BUMP_NICAS
  bump:
    datadir: ./bump
    strategy: specific_univariate
    load_nicas_local: true
    grids:
    - prefix: bump3d
      variables: [socn, tocn]
    - prefix: bump2d
      variables: [cicen, hicen, ssh]
saber outer blocks:
  - saber block name: StdDev
    input fields:
    - parameter: StdDev
      file:
        read_from_file: 1
        basename: ./
        ocn_filename: ocn.bkgerr_stddev.incr.$(ATM_WINDOW_BEGIN).nc
        ice_filename: ice.bkgerr_stddev.incr.$(ATM_WINDOW_BEGIN).nc
        date: $(ATM_WINDOW_BEGIN)
# TODO (G): Below change of variable is not ordered properly after processing this yaml
# TODO (G): failing in multiply inverse, check why eventually.
#linear variable change:
#  input variables: [socn, tocn, ssh, cicen, hicen]
#  output variables: [socn, tocn, ssh, cicen, hicen]
#  linear variable changes:
#  - linear variable change name: BalanceSOCA
#    dsdtmax: 0.1
#    dsdzmin: 3.0e-6
#    dtdzmin: 1.0e-6
#    nlayers: 10
